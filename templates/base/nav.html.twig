<link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<!------ Include the above in your HEAD tag ---------->

<div class="navbar navbar-expand-md navbar-dark bg-dark mb-4" role="navigation">
    <a class="navbar-brand" href="/">IQuestions</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Accueil<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/quizz">Liste des Quiz</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">À propos</a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/infos">Informations</a>
                    <a class="dropdown-item" href="/contact">Nous contacter</a>
               </div>
            </li>

        </ul>
        <form class="form-inline mt-2 mt-md-0">
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %} 
                 <a class="btn btn-outline-light" href="{{ path('admin') }}">Admin</a>
                {% endif %}
            <a class="btn btn-outline-danger" href="{{ path('app_user_profil') }}">Profil</a>
            <a class="btn btn-outline-danger" href="{{ path('app_logout') }}">Deconnexion</a>
            {% else %}
            <button class="btn btn-dark" type="button" onclick=window.location.href='register' value="register">S'inscrire</button>
            <button class="btn btn-dark" type="button" onclick=window.location.href='login' value="login">Se connecter</button>
            {% endif %}
            
        </form>
    </div>
</div>