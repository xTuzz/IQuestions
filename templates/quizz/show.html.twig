{% extends 'base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <div class="container">
    <h3> {{ quizz.Title }} </h3>
    <p> {{quizz.Description}} </p>
    <table class="table">
        <tbody>
            <tr>
                <th>Theme</th>
                <td>{{ quizz.Theme }}</td>
            </tr>
            <tr>
                <th>Difficulté</th>
                <td>{{ quizz.Difficulty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quizz_edit', {'id': quizz.id}) }}">modifier</a>
    </div>

    <div class="container">
        <h3> Questions : </h3>
        <table class="table">
  <thead>
    <tr>
      <th scope="col">Question</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for question in questions %}
    <tr>
        <th>{{question.wording}}</th>
        <td>Mark</td>
    </tr>
    {% endfor %}
  </tbody>
</table>

    <a href="{{ path('app_questions_new', {'quizz': quizz}) }}">ajouter une question</a>
    </div>

    {{ include('quizz/_delete_form.html.twig') }}
{% endblock %}
