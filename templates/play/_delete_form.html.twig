<form method="post" action="{{ path('app_play_delete', {'id': play.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ play.id) }}">
    <button class="btn">Delete</button>
</form>
