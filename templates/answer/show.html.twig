{% extends 'base.html.twig' %}

{% block title %}Réponse{% endblock %}

{% block body %}
    <h3>Réponse</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>Réponse Utilisateur</th>
                <td>{{ answer.AnswerUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_answer_index') }}">Retour à la liste</a>

    <a href="{{ path('app_answer_edit', {'id': answer.id}) }}">modifier</a>

    {{ include('answer/_delete_form.html.twig') }}
{% endblock %}
